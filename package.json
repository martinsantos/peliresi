{
    "name": "trazabilidad-rrpp-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "install": "cd backend && npm install",
        "build": "cd backend && npx prisma generate && npm run build",
        "start": "cd backend && npx prisma migrate deploy && node dist/index.js"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}