# Keep API calls same-origin so the same build works on the government domain,
# preview, and through a VPN/SSH tunnel without cross-host drift.
VITE_API_URL=/api
VITE_DEMO_MODE=false
VITE_TURNSTILE_SITE_KEY=
